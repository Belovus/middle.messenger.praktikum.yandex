<main class="chats">
    <div class="chats__left-panel">
        <div class="chats__link-wrapper">
            <div class="chats__link" ref="settingsLink">
                <a>Профиль</a>
                <img src="../../../static/chats-arrow.svg" alt="arrow" />
            </div>
        </div>
        <div class="chats__filter">
            <label>
                <input type="text" class="chats__filter-input" placeholder="Поиск" ref="chatSearchInput" />
            </label>
            <button class="chats__filter-button" ref="createChatButton">
                <img src="../../../static/add-white.svg" alt="add" />
            </button>
        </div>
        <ul class="chats__list">
            {{#each chats}}
            <div class="chats__divider"></div>
            {{{ ChatItem
                id=id
                title=title
                message=message
                time=time
                avatar=avatar
                unreadCount=unreadCount
                selected=selected
            }}}
            {{/each}}
            {{#unless chats.length}}
            <div class="chats__divider"></div>
            {{/unless}}
        </ul>
    </div>
    <div class="chat">
        {{#if selectedChatId}}
            <div class="chat__header">
            <div class="chat__header-info" ref="infoOptionsList">
                <div
                    class="chat__header-avatar"
                    {{#if selectedChat.avatar}}style="background-image: url('{{selectedChat.avatar}}'); background-size: cover;"{{/if}}
                ></div>
                <div class="chat__header-meta">
                    <h3 class="chat__header-name">{{ selectedChat.title }}</h3>
                    <span class="chat__header-count">Кол-во участников: {{ chatUsersCount }}</span>
                </div>
            </div>
            <div class="chat__header-info-options-list" ref="infoOptionsListBlock">
                <div class="chat__header-info-options-list-avatar">
                    {{{ AvatarEditor size="small" avatar=selectedChat.avatar }}}
                </div>
                <ul>
                    {{#each chatUsers }}
                        <li>
                            <div class="chat__header-info-options-list-user-avatar" {{#if avatar}}style="background-image: url('{{avatar}}'); background-size: cover;"{{/if}}>
                                {{#unless avatar}}
                                    <img
                                        src="../../../static/avatar.svg"
                                        alt="avatar"
                                    />
                                {{/unless}}
                            </div>
                            <div>
                                <h4 class="chat__header-info-options-list-name">{{ first_name }} {{ second_name }}</h4>
                                <span>{{ login }}</span>
                            </div>
                            <img class="chat__header-info-options-list-cross" src="../../../static/cross-red.svg" alt="cross" data-user-login={{ login }} />
                        </li>
                    {{/each}}
                </ul>
            </div>
            <div class="chat__header-options" ref="headerOptionsToggle">
                <img src="../../../static/profile-header-dots.svg" alt="dots" />
                <ul class="chat__header-options-list" ref="headerOptionsToggleBlock">
                    <li ref="addUserOption">
                        <div class="chat__header-options-img">
                            <img src="../../../static/add.svg" alt="add" />
                        </div>
                        <span>Добавить пользователя</span>
                    </li>
                    <div class="chat__header-options-divider"></div>
                    <li ref="removeChatOption">
                        <div class="chat__header-options-img">
                            <img src="../../../static/trash.svg" alt="delete" />
                        </div>
                        <span>Удалить чат</span>
                    </li>
                </ul>
            </div>
            </div>
            <div class="chat__body">
                <div class="chat__body-messages" ref="messagesList">
                    {{#each messages}}
                    {{{ Message
                        message=content
                        time=(formatTime time)
                        isOwn=isOwn
                    }}}
                    {{/each}}
                </div>
            </div>
            <form class="chat__footer">
                <div class="chat__footer-options" ref="bottomOptions">
                    <ul class="chat__footer-options-list" ref="bottomOptionsToggleBlock">
                        <li>
                            <img src="../../../static/photo-or-video.svg" alt="photo" />
                            <span>Фото или видео</span>
                        </li>
                        <div class="chat__footer-options-divider"></div>
                        <li>
                            <img src="../../../static/file.svg" alt="file" />
                            <span>Файл</span>
                        </li>
                        <div class="chat__footer-options-divider"></div>
                        <li>
                            <img src="../../../static/location.svg" alt="location" />
                            <span>Локация</span>
                        </li>
                    </ul>
                    <img src="../../../static/profile-footer-clip.svg" alt="clip" ref="bottomOptionsToggle" />
                </div>
                <input type="text" class="chat__footer-input" placeholder="Сообщение" name="message" />
                <button class="chat__footer-send-button" type="submit">
                    <img src="../../../static/arrow.svg" alt="send" />
                </button>
            </form>
        {{else}}
            <div class="chat__placeholder">
                <span>Выберите чат чтобы отправить сообщение</span>
            </div>
        {{/if}}
    </div>

    {{{ Modal
        isOpen=(equal activeModal "createChat")
        title="Создать чат"
        label="Название чата"
        fieldId="title"
        buttonText="Создать"
        crossRef="createChatCross"
        formRef="createChatForm"
        fieldRef="title"
    }}}

    {{{ Modal
        isOpen=(equal activeModal "addUser")
        title="Добавить пользователя"
        label="Логин пользователя"
        fieldId="login"
        buttonText="Добавить"
        crossRef="addUserCross"
        formRef="addUserForm"
        fieldRef="login"
    }}}
</main>
